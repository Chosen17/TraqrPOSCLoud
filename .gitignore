# Environment (secrets)
.env
.env.local
.env.*.local

# Build
target/

# Node / Tailwind
node_modules/
web/public/css/output.css

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Debug
*.log

# User uploads (avatars, blog images) — keep directory, don't commit contents
uploads/*
!uploads/.gitkeep
